# SchoNavi V0.2 地基增量 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 V0.2 全部功能铺好基础设施——本地持久化（`LocalStore`）、外链打开（`LinkLauncher`）、已核实的 Markdown 渲染（`gpt_markdown`），且不触碰 V0.1 已验证的推荐链路。

**Architecture:** 延续 V0.1 三层 + 横切 core 的结构。新增三块**纯基础设施**，均以「抽象接口（domain 风格放在 core）+ 具体实现 + Riverpod provider + 可注入假实现」的方式落地，让后续 feature（收藏/历史/登录/对话/主页跳转）只依赖接口、便于单测。`LocalStore` 经 `SharedPreferences` 实现，在 `main()` 异步初始化后用 provider override 注入；`LinkLauncher` 包一层 `url_launcher`；Markdown 选定 `gpt_markdown` 并以冒烟测试核实可用性。

**Tech Stack:** Flutter 3.44.1 / Dart 3.12.1；新增 `shared_preferences`、`url_launcher`、`gpt_markdown ^1.1.7`（`flutter_markdown_plus` 为备选）。

**Spec 依据:** `docs/superpowers/specs/2026-06-07-schonavi-master-design.md`（§2 技术栈、§3.2 目录、§6.3 本地存储）与 `...-v0.2-mvp-spec.md`（§2.1/2.3/2.5/2.6/2.7 的前置基础设施）。

**前置条件（已完成）:** V0.1 实现已提交（`d414ea4`），`flutter analyze` 无 error、36 个测试全绿。本增量在 `feat/v0.1-prototype` 分支继续。

**与 spec 的偏差（已记录）:**
1. **Markdown 包**：主设计 §2 要求「落地前在 foundation plan 里核实」。`flutter_markdown` 已于 2025-05 被 Google 官方标记 discontinued。本计划选定 **`gpt_markdown`**（面向 AI/LLM 输出、`GptMarkdown` 单 widget、内置 LaTeX 与可选中文文本选择、活跃维护），契合对话追问的助手回答渲染；若解析或渲染异常，**回退到 `flutter_markdown_plus`**（官方社区续作，widget 名 `Markdown`/`MarkdownBody`）。
2. **DioClient 推迟到 V1.0**（YAGNI）：V0.2 全程走 mock 数据源，没有任何 http 调用方；现在建 `DioClient` 会是无调用方、难有意义测试的死代码。主设计 §2.7 也把 `Http*Repository` 归入 V1.0。**若需要现在就落一个「带错误码映射拦截器」的 DioClient 骨架（可用 dio mock adapter 测试映射逻辑），可加一个独立任务——本计划默认不含。**
3. `gpt_markdown` 在 V0.2 的对话功能落地前不会被 App 代码使用，但本增量加入依赖并以冒烟测试引用它，目的正是「在当前 SDK 上核实可解析、可渲染」并锁定版本——这是主设计明确要求的 foundation 动作，不算投机。

---

## 文件结构

| 文件 | 职责 |
|---|---|
| `pubspec.yaml` | 新增 `shared_preferences`、`url_launcher`、`gpt_markdown` 依赖 |
| `lib/core/storage/local_store.dart` | `LocalStore` 抽象接口（string / bool / json-map / json-list 读写 + remove/clear/containsKey） |
| `lib/core/storage/shared_preferences_local_store.dart` | 基于 `SharedPreferences` 的实现（json 经 `jsonEncode/Decode` 存为字符串） |
| `lib/core/launcher/link_launcher.dart` | `LinkLauncher` 抽象接口 + `LaunchResult` 枚举 |
| `lib/core/launcher/url_launcher_link_launcher.dart` | 基于 `url_launcher` 的实现（含 null/空/非 http 守卫） |
| `lib/core/di/providers.dart` | **修改**：新增 `sharedPreferencesProvider`、`localStoreProvider`、`linkLauncherProvider` |
| `lib/main.dart` | **修改**：异步初始化 `SharedPreferences` 并 override 注入 |
| `test/core/storage/shared_preferences_local_store_test.dart` | LocalStore 实现的往返/删除/清空单测 |
| `test/core/di/infra_providers_test.dart` | 三个基础设施 provider 的接线单测 |
| `test/core/launcher/url_launcher_link_launcher_test.dart` | LinkLauncher 守卫分支单测 |
| `test/core/markdown_smoke_test.dart` | `gpt_markdown` 可用性冒烟测试 |

> 不新增 feature、不改动任何 V0.1 业务文件（除 `main.dart` 的初始化接线与 `di/providers.dart` 的新增）。V0.1 既有测试须保持全绿。

---

## Task 1: 依赖接入与 Markdown 渲染核实

**Files:**
- Modify: `pubspec.yaml`
- Create: `test/core/markdown_smoke_test.dart`

- [ ] **Step 1: 加依赖**

Run:
```bash
flutter pub add shared_preferences url_launcher gpt_markdown
```
Expected: `pubspec.yaml` 的 `dependencies:` 下新增三行（约 `shared_preferences: ^2.5.x`、`url_launcher: ^6.3.x`、`gpt_markdown: ^1.1.7`，以 `flutter pub get` 解析为准）；`flutter pub get` 成功。**`pub get` 成功本身即「`gpt_markdown` 与 Dart 3.12 兼容」的核实。**

- [ ] **Step 2: 写 Markdown 冒烟测试 `test/core/markdown_smoke_test.dart`**

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:gpt_markdown/gpt_markdown.dart';

void main() {
  testWidgets('gpt_markdown 可用：能渲染 markdown 且不抛异常', (tester) async {
    await tester.pumpWidget(
      MaterialApp(
        home: Scaffold(
          body: GptMarkdown('你好 **世界**，这是 `inline code` 与一段列表：\n- A\n- B'),
        ),
      ),
    );
    await tester.pumpAndSettle();

    // 核心断言：包能解析+构建渲染树而不报错。
    expect(tester.takeException(), isNull);
    expect(find.byType(GptMarkdown), findsOneWidget);
    // 次要断言：原始 markdown 标记不应作为整段字面文本出现。
    expect(find.text('你好 **世界**，这是 `inline code` 与一段列表：\n- A\n- B'),
        findsNothing);
  });
}
```

> 若 `gpt_markdown` 的 widget 名或位置参数与此不符（以 pub.dev 1.1.7 文档为准：主 widget `GptMarkdown`、首个位置参数为 markdown 字符串），按实际 API 修正导入与调用；若 `pub get` 或渲染失败，执行偏差 1 的回退：`flutter pub remove gpt_markdown && flutter pub add flutter_markdown_plus`，并把测试改为 `MarkdownBody(data: '...')`。

- [ ] **Step 3: 运行测试，确认通过**

Run: `flutter test test/core/markdown_smoke_test.dart`
Expected: PASS。

- [ ] **Step 4: 提交**

```bash
git add pubspec.yaml pubspec.lock test/core/markdown_smoke_test.dart
git commit -m "chore: add shared_preferences/url_launcher/gpt_markdown + markdown smoke test"
```

---

## Task 2: LocalStore 抽象接口

**Files:**
- Create: `lib/core/storage/local_store.dart`

> 抽象契约无独立单测，由 Task 3 的实现测试覆盖；本任务以 `flutter analyze` 通过为验证（参照 V0.1 Task 5 的做法）。

- [ ] **Step 1: 实现 `lib/core/storage/local_store.dart`**

```dart
/// 本地键值持久化抽象（主设计 §6.3）。MVP 由 SharedPreferences 实现，
/// 后续可换 Hive。收藏 / 历史 / 首启标记 / 登录 token / 用户信息均经此存取。
///
/// 读方法同步返回（实现层在初始化后持有缓存）；写方法异步。
/// 缺失或解析失败一律返回 null，绝不抛出，便于上层降级到「暂无信息」。
abstract interface class LocalStore {
  String? getString(String key);
  Future<void> setString(String key, String value);

  bool? getBool(String key);
  Future<void> setBool(String key, bool value);

  /// 读/写一个 JSON 对象（内部以 jsonEncode 存为字符串）。
  Map<String, dynamic>? getJson(String key);
  Future<void> setJson(String key, Map<String, dynamic> value);

  /// 读/写一个 JSON 数组（收藏列表 / 历史列表用）。
  List<dynamic>? getJsonList(String key);
  Future<void> setJsonList(String key, List<dynamic> value);

  bool containsKey(String key);
  Future<void> remove(String key);
  Future<void> clear();
}
```

- [ ] **Step 2: 验证并提交**

Run: `flutter analyze`
Expected: 无 error。
```bash
git add lib/core/storage/local_store.dart
git commit -m "feat: LocalStore abstraction for V0.2 local persistence"
```

---

## Task 3: SharedPreferencesLocalStore 实现

**Files:**
- Create: `lib/core/storage/shared_preferences_local_store.dart`
- Create: `test/core/storage/shared_preferences_local_store_test.dart`

- [ ] **Step 1: 写失败测试 `test/core/storage/shared_preferences_local_store_test.dart`**

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:shared_preferences/shared_preferences.dart';
import 'package:scho_navi/core/storage/local_store.dart';
import 'package:scho_navi/core/storage/shared_preferences_local_store.dart';

void main() {
  late LocalStore store;

  setUp(() async {
    SharedPreferences.setMockInitialValues(<String, Object>{});
    final prefs = await SharedPreferences.getInstance();
    store = SharedPreferencesLocalStore(prefs);
  });

  test('string 往返；缺失返回 null', () async {
    expect(store.getString('k'), isNull);
    await store.setString('k', 'v');
    expect(store.getString('k'), 'v');
  });

  test('bool 往返（首启标记场景）', () async {
    expect(store.getBool('seenOnboarding'), isNull);
    await store.setBool('seenOnboarding', true);
    expect(store.getBool('seenOnboarding'), true);
  });

  test('json 对象往返（用户信息场景）', () async {
    await store.setJson('user', <String, dynamic>{'id': 'u1', 'isGuest': true});
    expect(store.getJson('user'), <String, dynamic>{'id': 'u1', 'isGuest': true});
  });

  test('json 数组往返（收藏/历史场景）', () async {
    await store.setJsonList('favs', <dynamic>[
      <String, dynamic>{'professorId': 'p_001'},
      <String, dynamic>{'professorId': 'p_002'},
    ]);
    final list = store.getJsonList('favs');
    expect(list, hasLength(2));
    expect((list!.first as Map)['professorId'], 'p_001');
  });

  test('损坏/缺失的 json 返回 null 而非抛出', () async {
    expect(store.getJson('missing'), isNull);
    expect(store.getJsonList('missing'), isNull);
    await store.setString('notJson', '{ this is not json');
    expect(store.getJson('notJson'), isNull);
    expect(store.getJsonList('notJson'), isNull);
  });

  test('remove 删除单键', () async {
    await store.setString('k', 'v');
    await store.remove('k');
    expect(store.getString('k'), isNull);
    expect(store.containsKey('k'), isFalse);
  });

  test('clear 清空全部', () async {
    await store.setString('a', '1');
    await store.setBool('b', true);
    await store.clear();
    expect(store.containsKey('a'), isFalse);
    expect(store.containsKey('b'), isFalse);
  });
}
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `flutter test test/core/storage/shared_preferences_local_store_test.dart`
Expected: FAIL（`shared_preferences_local_store.dart` 不存在）。

- [ ] **Step 3: 实现 `lib/core/storage/shared_preferences_local_store.dart`**

```dart
import 'dart:convert';

import 'package:shared_preferences/shared_preferences.dart';

import 'local_store.dart';

/// [LocalStore] 的 SharedPreferences 实现。
/// JSON 对象/数组统一经 [jsonEncode] 存为字符串，读时容错解码。
class SharedPreferencesLocalStore implements LocalStore {
  SharedPreferencesLocalStore(this._prefs);

  final SharedPreferences _prefs;

  @override
  String? getString(String key) => _prefs.getString(key);

  @override
  Future<void> setString(String key, String value) =>
      _prefs.setString(key, value);

  @override
  bool? getBool(String key) => _prefs.getBool(key);

  @override
  Future<void> setBool(String key, bool value) => _prefs.setBool(key, value);

  @override
  Map<String, dynamic>? getJson(String key) {
    final raw = _prefs.getString(key);
    if (raw == null) return null;
    try {
      final decoded = jsonDecode(raw);
      return decoded is Map<String, dynamic> ? decoded : null;
    } catch (_) {
      return null;
    }
  }

  @override
  Future<void> setJson(String key, Map<String, dynamic> value) =>
      _prefs.setString(key, jsonEncode(value));

  @override
  List<dynamic>? getJsonList(String key) {
    final raw = _prefs.getString(key);
    if (raw == null) return null;
    try {
      final decoded = jsonDecode(raw);
      return decoded is List ? decoded : null;
    } catch (_) {
      return null;
    }
  }

  @override
  Future<void> setJsonList(String key, List<dynamic> value) =>
      _prefs.setString(key, jsonEncode(value));

  @override
  bool containsKey(String key) => _prefs.containsKey(key);

  @override
  Future<void> remove(String key) => _prefs.remove(key);

  @override
  Future<void> clear() => _prefs.clear();
}
```

- [ ] **Step 4: 运行测试，确认通过**

Run: `flutter test test/core/storage/shared_preferences_local_store_test.dart`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add lib/core/storage/shared_preferences_local_store.dart test/core/storage/shared_preferences_local_store_test.dart
git commit -m "feat: SharedPreferences-backed LocalStore with round-trip tests"
```

---

## Task 4: DI 接线 + main() 异步初始化

**Files:**
- Modify: `lib/core/di/providers.dart`
- Modify: `lib/main.dart`
- Create: `test/core/di/infra_providers_test.dart`

- [ ] **Step 1: 在 `lib/core/di/providers.dart` 顶部追加 import**

把文件第 1–8 行的 import 区改为（新增 3 行 import）：
```dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:shared_preferences/shared_preferences.dart';

import '../../data/mock/mock_db.dart';
import '../../data/mock/mock_professor_repository.dart';
import '../../data/mock/mock_recommendation_repository.dart';
import '../../domain/repositories/professor_repository.dart';
import '../../domain/repositories/recommendation_repository.dart';
import '../config/app_config.dart';
import '../launcher/link_launcher.dart';
import '../launcher/url_launcher_link_launcher.dart';
import '../storage/local_store.dart';
import '../storage/shared_preferences_local_store.dart';
```

- [ ] **Step 2: 在 `lib/core/di/providers.dart` 末尾追加三个 provider**

在文件末尾（`professorRepositoryProvider` 之后）追加：
```dart

/// 在 main() 中用 SharedPreferences.getInstance() 的结果 override。
/// 未 override 直接读取会抛错，提醒接线缺失。
final sharedPreferencesProvider = Provider<SharedPreferences>(
  (ref) => throw UnimplementedError(
    'sharedPreferencesProvider must be overridden in main() with '
    'SharedPreferences.getInstance()',
  ),
);

/// 全应用本地持久化入口。收藏/历史/登录态/首启标记均经此存取。
final localStoreProvider = Provider<LocalStore>(
  (ref) => SharedPreferencesLocalStore(ref.watch(sharedPreferencesProvider)),
);

/// 外链打开（教师主页等）。feature 层只依赖 [LinkLauncher] 接口，便于注入假实现。
final linkLauncherProvider = Provider<LinkLauncher>(
  (ref) => const UrlLauncherLinkLauncher(),
);
```

- [ ] **Step 3: 改写 `lib/main.dart` 为异步初始化 + override 注入**

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:shared_preferences/shared_preferences.dart';

import 'app.dart';
import 'core/di/providers.dart';

Future<void> main() async {
  WidgetsFlutterBinding.ensureInitialized();
  final prefs = await SharedPreferences.getInstance();
  runApp(
    ProviderScope(
      overrides: [sharedPreferencesProvider.overrideWithValue(prefs)],
      child: const SchoNaviApp(),
    ),
  );
}
```

- [ ] **Step 4: 写并运行接线测试 `test/core/di/infra_providers_test.dart`**

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:shared_preferences/shared_preferences.dart';
import 'package:scho_navi/core/di/providers.dart';
import 'package:scho_navi/core/launcher/link_launcher.dart';
import 'package:scho_navi/core/launcher/url_launcher_link_launcher.dart';
import 'package:scho_navi/core/storage/local_store.dart';
import 'package:scho_navi/core/storage/shared_preferences_local_store.dart';

void main() {
  test('未 override 时 sharedPreferencesProvider 抛 UnimplementedError', () {
    final container = ProviderContainer();
    addTearDown(container.dispose);
    expect(
      () => container.read(sharedPreferencesProvider),
      throwsA(isA<UnimplementedError>()),
    );
  });

  test('override 后 localStoreProvider 产出 SharedPreferencesLocalStore', () async {
    SharedPreferences.setMockInitialValues(<String, Object>{});
    final prefs = await SharedPreferences.getInstance();
    final container = ProviderContainer(overrides: [
      sharedPreferencesProvider.overrideWithValue(prefs),
    ]);
    addTearDown(container.dispose);
    final store = container.read(localStoreProvider);
    expect(store, isA<SharedPreferencesLocalStore>());
    expect(store, isA<LocalStore>());
  });

  test('linkLauncherProvider 产出 UrlLauncherLinkLauncher', () {
    final container = ProviderContainer();
    addTearDown(container.dispose);
    final launcher = container.read(linkLauncherProvider);
    expect(launcher, isA<UrlLauncherLinkLauncher>());
    expect(launcher, isA<LinkLauncher>());
  });
}
```

> 本测试依赖 Task 5 创建的 `link_launcher.dart` / `url_launcher_link_launcher.dart`。若按顺序执行，请先做 Task 5 的 Step 1–2（建接口与实现），或把 `linkLauncherProvider` 相关断言留到 Task 5 完成后再启用。推荐执行顺序：Task 5 → Task 4，或合并两者于同一轮。

Run: `flutter test test/core/di/infra_providers_test.dart`
Expected: PASS。

- [ ] **Step 5: 回归——确认 V0.1 既有测试仍全绿**

Run: `flutter test test/widget_test.dart test/app_e2e_test.dart`
Expected: PASS。（V0.1 代码不读取 `sharedPreferencesProvider`，故直接 `pumpWidget(ProviderScope(child: SchoNaviApp()))` 不触发 override 缺失异常。）

- [ ] **Step 6: 提交**

```bash
git add lib/core/di/providers.dart lib/main.dart test/core/di/infra_providers_test.dart
git commit -m "feat: wire localStore/sharedPreferences/linkLauncher providers + async main init"
```

---

## Task 5: LinkLauncher 抽象与 url_launcher 实现

**Files:**
- Create: `lib/core/launcher/link_launcher.dart`
- Create: `lib/core/launcher/url_launcher_link_launcher.dart`
- Create: `test/core/launcher/url_launcher_link_launcher_test.dart`

> 执行顺序提示：本任务的接口/实现是 Task 4 Step 4 `linkLauncherProvider` 断言的前置。建议先完成本任务的 Step 1–2，再回到 Task 4，或将两任务合并为一轮。

- [ ] **Step 1: 实现 `lib/core/launcher/link_launcher.dart`**

```dart
/// 打开外链的结果。UI 据此给出对应中文提示（V0.2 §2.5）：
/// noUrl → 「暂无主页信息」；failed → 「主页可能已失效，可通过学校官网确认」。
enum LaunchResult { success, noUrl, failed }

/// 外链打开抽象。feature 层只依赖此接口，便于在 widget 测试中注入假实现。
abstract interface class LinkLauncher {
  /// 用系统浏览器打开 [url]。
  /// - null / 空白 → [LaunchResult.noUrl]
  /// - 非法 / 非 http(s) / 打开失败 → [LaunchResult.failed]
  /// - 成功 → [LaunchResult.success]
  Future<LaunchResult> open(String? url);
}
```

- [ ] **Step 2: 实现 `lib/core/launcher/url_launcher_link_launcher.dart`**

```dart
import 'package:url_launcher/url_launcher.dart';

import 'link_launcher.dart';

/// 基于 url_launcher 的实现，用系统浏览器（externalApplication）打开。
class UrlLauncherLinkLauncher implements LinkLauncher {
  const UrlLauncherLinkLauncher();

  @override
  Future<LaunchResult> open(String? url) async {
    final trimmed = url?.trim() ?? '';
    if (trimmed.isEmpty) return LaunchResult.noUrl;

    final uri = Uri.tryParse(trimmed);
    if (uri == null || (!uri.isScheme('http') && !uri.isScheme('https'))) {
      return LaunchResult.failed;
    }

    try {
      final ok = await launchUrl(uri, mode: LaunchMode.externalApplication);
      return ok ? LaunchResult.success : LaunchResult.failed;
    } catch (_) {
      return LaunchResult.failed;
    }
  }
}
```

- [ ] **Step 3: 写失败测试 `test/core/launcher/url_launcher_link_launcher_test.dart`**

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:scho_navi/core/launcher/link_launcher.dart';
import 'package:scho_navi/core/launcher/url_launcher_link_launcher.dart';

void main() {
  const launcher = UrlLauncherLinkLauncher();

  test('null url → noUrl', () async {
    expect(await launcher.open(null), LaunchResult.noUrl);
  });

  test('空白 url → noUrl', () async {
    expect(await launcher.open('   '), LaunchResult.noUrl);
  });

  test('缺 scheme 的 url → failed', () async {
    expect(await launcher.open('example.edu.cn/zhangsan'), LaunchResult.failed);
  });

  test('非 http(s) scheme → failed', () async {
    expect(await launcher.open('ftp://example.edu.cn'), LaunchResult.failed);
  });
}
```

> 这 4 个断言都在调用 `launchUrl` **之前**返回，因此不触发平台通道，可在纯 VM 下运行。
> `success` / 平台打开失败的路径依赖 `UrlLauncherPlatform`，放到 §2.5「教师主页跳转」feature 计划里覆盖：feature 的 widget 测试注入一个 `LinkLauncher` 假实现，真机/模拟器再做人工冒烟。

- [ ] **Step 4: 运行测试，确认通过**

Run: `flutter test test/core/launcher/url_launcher_link_launcher_test.dart`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add lib/core/launcher/ test/core/launcher/
git commit -m "feat: LinkLauncher abstraction + url_launcher impl with guard tests"
```

---

## Task 6: 收尾全量验证

**Files:** 无（仅验证）

- [ ] **Step 1: 全量分析与测试**

Run:
```bash
flutter analyze && flutter test
```
Expected: analyze 无 error；全部测试 PASS——V0.1 的 36 个 + 本增量新增（markdown 冒烟 1、LocalStore 实现 7、infra providers 3、LinkLauncher 守卫 4 ≈ 15 个）。

- [ ] **Step 2: 确认依赖锁定与无残留改动**

Run:
```bash
git status --short
```
Expected: 工作区干净（仅可能余下未跟踪的 `.agents/`）；`pubspec.lock` 已随 Task 1 提交。

- [ ] **Step 3: 标记增量完成**

本增量交付：`LocalStore`（含实现 + provider）、`LinkLauncher`（含实现 + provider）、`gpt_markdown` 已核实可用。**已解锁**的后续 V0.2 feature 计划：
- 收藏（`features/favorite`）、历史（`features/history`）、首启标记/引导（`features/onboarding`）、登录 token/用户（`features/auth`）→ 依赖 `localStoreProvider`。
- 教师主页跳转（§2.5）→ 依赖 `linkLauncherProvider`。
- 对话追问（`features/chat`）的 Markdown 渲染 → 用 `gpt_markdown`。

> 本增量无新增 UI，模拟器无新增可演示项；人工冒烟随首个依赖它的 feature 一并进行。

---

## 自查（Self-Review）记录

- **Spec 覆盖**：本增量对应主设计 §2（技术栈：shared_preferences/url_launcher/markdown 包落地核实）、§3.2（目录新增 `core/storage`、`core/launcher`）、§6.3（LocalStore 抽象 + SharedPreferences 实现 + 「云同步就绪」的本地基础）；并作为 V0.2 §2.1/2.3/2.5/2.6/2.7 的前置基础设施。DioClient（§2.7）按偏差 2 显式推迟 V1.0。
- **占位扫描**：无 TBD/TODO；所有步骤含可运行命令与完整代码；markdown 冒烟测试给出主断言（构建不抛异常）与 API 不符时的修正/回退说明。
- **类型一致性**：`LocalStore`（getString/setString、getBool/setBool、getJson/setJson、getJsonList/setJsonList、containsKey、remove、clear）在接口（Task 2）、实现（Task 3）、测试（Task 3/4）三处签名一致；`SharedPreferencesLocalStore` 构造签名 `(SharedPreferences)` 在实现、Task 3 测试、`localStoreProvider`（Task 4）一致；`LinkLauncher.open(String?) → Future<LaunchResult>` 与 `LaunchResult{success,noUrl,failed}` 在接口（Task 5 Step1）、实现（Step2）、测试（Step3）、provider（Task 4）一致；provider 命名 `sharedPreferencesProvider`/`localStoreProvider`/`linkLauncherProvider` 跨 Task 4/5 与测试一致。
- **执行顺序依赖**：Task 4 Step 4 的 `linkLauncherProvider` 断言依赖 Task 5 的接口/实现——已在两处显式标注「先 Task 5 Step1–2 或合并执行」。
- **不回归 V0.1**：除 `main.dart` 接线与 `di/providers.dart` 追加外不改动 V0.1 文件；Task 4 Step 5 专门回归既有 `widget_test`/`app_e2e_test`。
